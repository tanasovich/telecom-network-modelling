<?xml version="1.0" encoding="utf-8" ?>
<configuration>
    <appSettings>
        <!-- N -->
        <add key="fourierTransformBase" value="4096" />
        <!-- n -->
        <add key="carrierFrequencyMaxNumber" value="2005" />
        <!-- L -->
        <add key="protectionIntervalSamplesNumber" value="320" />
        <!-- m -->
        <add key="firstChannelNumber" value="43" />
        <!-- R -->
        <add key="impulseReactionLength" value="954" />
        <!-- from_lt -->
        <add key="firstSample" value="0" />
        <!-- until_lt -->
        <add key="lastSample" value="500" />
    </appSettings>
</configuration>
